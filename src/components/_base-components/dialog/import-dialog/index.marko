<widget instanceId=state.instanceId>
    <@content>
        <if(!state.loading)>
            <main-form formId=state.formId key="input-configuration-form"/>
            <!-- <form.form autocomplete="off">
                <if(state.bulkManager)>
                    <bulk-form bulkManager=state.bulkManager/>
                </if>
            </form> -->
        </if>
        <else>
            <loading-spinner/>
        </else>
    </@content>
</widget>
