<if(state.formField)>
    <input.input-style value=state.currentValue type="text" invalid=state.invalid placeholder=input.placeholder
        required=input.required on-keydown('keyDown') on-change('valueChanged') on-blur('focusLost')/>
</if>
