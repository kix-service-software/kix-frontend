<if(state.formField)>
    <input.input-style class={invalid: state.invalid} value=state.currentValue type="text"
        placeholder=(state.formField.required ? state.placeholder : "") required=input.required on-keydown('keyDown')
        on-change('valueChanged') on-blur('focusLost')/>
</if>
