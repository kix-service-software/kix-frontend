<if(state.field)>
    <input.input-style class={invalid: state.invalid} value=state.currentValue type=state.inputType
        placeholder=(state.field.required ? state.placeholder : "") autocomplete=component.getAutoCompleteOption()
        required=input.required on-keydown('keyDown') on-change('valueChanged')/>
</if>
