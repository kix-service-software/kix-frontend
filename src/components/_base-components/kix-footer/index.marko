<div.kix-footer>
    <div.left-text>${input.leftText}</div>
    <div>
        <if(state.currentUserLogin)>
            ${state.kixProduct}${state.kixVersion}${state.buildNumber} |
            <translation-string pattern="Translatable#Logged in"/>
            ${" " + state.currentUserLogin} |
        </if>
        <translation-string pattern="Translatable#A product by c.a.p.e. IT GmbH"/>
    </div>
    <div.right-text>${input.rightText}</div>
</div>
