<if(state.show)>
    <div.object-info-overlay key="overlay" infoOverlay=true on-click("overlayClicked")>
        <include(state.content, state.data)/>
        <span.kix-icon-close on-click('closeOverlay')/>
    </div>
</if>
