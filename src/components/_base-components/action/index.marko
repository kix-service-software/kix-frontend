<if(state.action.displayText)>
    <div.action on-click('doAction') title=state.action.text>
        <span class=state.action.icon/>
        <span>${state.action.text}</span>
    </div>
</if>
<else>
    <span class=state.action.icon on-click('doAction') title=state.action.text/>
</else>
