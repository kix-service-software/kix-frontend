<div.key-value-pair.clearfix>
    <span.key title=component.getPropertyName()>${component.getPropertyName()}:</span>
    <span.info>
        <if(input.info)>
            <include(input.info)/>
        </if>
    </span>
    <span.value title=component.getPropertyDisplayText() class=component.getValueClasses()>
        ${component.getPropertyDisplayText()}
        <if(input.icon)>
            <include(input.icon)/>
        </if>
    </span>
</div>
