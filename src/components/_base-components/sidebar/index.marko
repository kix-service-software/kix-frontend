<if(state.showSidebar || state.showEmpty)>
    <div.sidebar-area>
        <for(sidebar in state.sidebars)>
            <div.sidebar>
                <include(component.getSidebarTemplate(sidebar.instanceId), { instanceId: sidebar.instanceId, minimizable: false, contextType: state.contextType })/>
            </div>
        </for>
    </div>
</if>
