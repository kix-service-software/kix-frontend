<div.ticket-article-content>
    <!-- TODO: durch $! unsicher, aber ansonsten ist es nur text, kein html -->
    <if(state.isContentHTML)>$!{state.content}</if>
    <else>${state.content}</else>
</div>
