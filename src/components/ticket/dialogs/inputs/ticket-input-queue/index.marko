<form-list
    nodes=state.nodes
    placeholder=state.placeholder
    selectedNodes=[state.currentNode]
    required=state.field.required
    on-focusLost("focusLost")
    on-nodesChanged("queueChanged")
    invalid=state.invalid
    multiselect=state.multiselect/>
