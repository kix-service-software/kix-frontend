<form-list placeholder=(state.field.required ? "Ansprechpartner zuordnen" : "") searchCallback=state.searchCallback
    autoCompleteConfiguration=state.autoCompleteConfiguration on-focusLost('focusLost')
    on-nodesChanged('contactChanged') selectedNodes=[state.currentNode] invalid=state.invalid autocomplete=true
    readonly=state.field.readonly/>
