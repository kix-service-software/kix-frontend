<if(state.serviceId)>
    <key-value-pair showKey=input.hasLabel>
        <@key>${state.label}</@key>
        <@value>
            <if(input.hasText)>
                <span.text title=state.displayValue>${state.displayValue}</span>
            </if>
            <if(input.hasIcon)>
                <icon object="IncidentState" objectId=state.incidentStateId title=state.displayValue/>
            </if>
        </@value>
    </key-value-pair>
</if>
