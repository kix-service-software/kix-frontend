<if(state.customer)>
    <for(groupTuple in state.customer.getCustomerInfoValues())>
        <div.customer-attribute-group>
            <for(attributeTuple in groupTuple[1])>
                <div title=((attributeTuple[0] + ": ") + attributeTuple[1])>
                    <span>${attributeTuple[0]}:</span>
                    ${attributeTuple[1]}
                </div>
            </for>
        </div>
    </for>
</if>
