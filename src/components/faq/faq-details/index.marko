<if(state.error)>
    <span.error>${state.error}</span>
</if>
<else-if(state.loading)>
    <loading-spinner/>
</else-if>
<else>
    <div.faq-details>
        <widget instanceId=state.instanceId minimizable=true title=component.getTitle() actions=component.getActions()
            contentActions=component.getFAQActions()>
            <@content>
                <ul.lane-list no-update>
                    <li>
                        <tab-container tabWidgets=state.tabWidgets title="FAQ-Informationen" showSidebar=false/>
                    </li>
                    <for(lane in state.lanes)>
                        <li.lane>
                            <include(component.getWidgetTemplate(lane.instanceId), { 
                                contextType: "MAIN",
                                instanceId: lane.instanceId, 
                                type: component.LANE_WIDGET_TYPE
                                })/>
                        </li>
                    </for>
                </ul>
            </@content>
        </widget>
        <include(component.getWidgetTemplate('faq-article-content-widget'), { 
            contextType: "MAIN",
            instanceId: 'faq-article-content-widget', 
            type: component.LANE_WIDGET_TYPE
        })/>
    </div>
</else>
