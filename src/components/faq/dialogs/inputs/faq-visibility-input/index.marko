<form-list nodes=state.nodes placeholder=(state.field.required ? state.field.label : "") on-focusLost('focusLost')
    on-nodesChanged('validChanged') selectedNodes=[state.currentNode] invalid=state.invalid
    readonly=state.field.readonly/>
