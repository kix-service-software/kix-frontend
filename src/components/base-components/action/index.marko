<span.action on-click('handleAction')>
    <span.kix-icon.icon-dummy/>
    <span>
        <if(state.running)>~~~</if>
        <else>${state.action.name}</else>
    </span>
</span>
<span>
    <if(state.action.useOverlay)>
        <action-overlay action=state.action contentData=data.contentData showOverlay=state.showActionOverlay
            on-runAction('runActionClicked') on-cancelAction('cancelActionClicked')/>
    </if>
</span>
