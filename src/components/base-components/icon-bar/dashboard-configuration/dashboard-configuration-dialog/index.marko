<div.widget.form-widget>
    <div.widget-header>
        <span.kix-icon.icon-dummy.icon-asterisk/>Auswahl der Widgets
    </div>
    <div.widget-content>
        <form>
            <if(state.error)>
                <div.create-error>${state.error.message}</div>
            </if>
            <div.form-group>
                <!-- TODO: use translation -->
                <h3>Explorer-Widgets</h3>
                <selection-with-properties title="Verfügbare Explorer" list=state.explorerList properties=state.explorerProperties/>
            </div>
            <div.form-group>
                <!-- TODO: use translation -->
                <h3>Content-Widgets</h3>
                <two-list-selection firstTitle="Verfügbare Widgets" firstList=state.contentFirstList secondTitle="Angezeigte Widgets"
                    secondList=state.contentSecondList properties=state.contentProperties firstListFixed=true
                    filterPlaceholder="Verfügbare Widgets filtern" size=4 firstMultiple=true secondMultiple=true/>
            </div>
            <div.form-group>
                <!-- TODO: use translation -->
                <h3>Sidebar-Widgets</h3>
                <two-list-selection firstTitle="Verfügbare Sideabrs" firstList=state.sidebarFirstList secondTitle="Angezeigte Sidebars"
                    secondList=state.sidebarSecondList properties=state.sidebarProperties
                    filterPlaceholder="Verfügbare Sidebars filtern" size=4 firstMultiple=true secondMultiple=true/>
            </div>
            <div.form-buttons>
                <!-- TODO: use translation -->
                <button on-click('Cancel')>Abbrechen</button>
                <button on-click('Submit')>Widgets auf ${input.dashboardName} platzieren</button>
            </div>
        </form>
    </div>
</div>
