<div.breadcrumb>
    <icon icon=state.icon/>
    <for(context in state.contexts | status-var=loop)>
        <link-target routingConfiguration=component.getRoutingConfiguration(context[0], loop.getIndex())>
            <@content>
                <span.context>${context[1]}</span>
            </@content>
        </link-target>
    </for>
</div>
