<div.breadcrumb>
    <icon icon=state.icon/>
    <for(context in state.contexts | status-var=loop)>
        <if((loop.getIndex()-1) !== state.contexts.length)>
            <span.context on-click('changeContext', context[0])>${context[1]}</span>
        </if>
    </for>
</div>
