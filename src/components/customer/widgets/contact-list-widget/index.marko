<widget instanceId=state.instanceId icon="kix-icon-man-bubble" title=state.title>
    <@headerFilter>
        <filter filterCount=state.filterCount on-filter('filter')/>
    </@headerFilter>
    <@content>
        <if(state.standardTable)>
            <standard-table standardTable=state.standardTable/>
        </if>
    </@content>
</widget>
