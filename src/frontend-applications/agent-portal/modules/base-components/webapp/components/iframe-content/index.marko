<iframe
    id=state.frameId
    class=[
        "w-100 bg-white",
        {
            "h-100": !state.calculateHeight,
        },
    ]
    sandbox="allow-same-origin allow-same-site-none-cookies"
    on-load("viewLoaded")/>
