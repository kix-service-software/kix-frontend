{% if parent.schema %}
    <p><strong>Schema</strong>:</p>
    <div class="schemas {%- if not noToggle %} toggleable {% endif %}">
        <pre><code>{{ parent.schema | escape }}</code></pre>
    </div>
{% endif %}